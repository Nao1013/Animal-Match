<header>
  <nav class="navbar navbar-expand navbar-info" style="background-color: sandybrown;">
    <div class="container">
      <%= link_to image_tag('logo.png', width: '40%', height: '50%', class: "ml-5"), root_path %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
        <ul class="navbar-nav">
          <!--施設側ログイン後表示-->
          <% if facility_signed_in? %>
            <div class="pl-1"></div>
              <li class="nav-item dropdown">
                <a class="nav-link" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  <i class="far fa-user-circle fa-3x bg-light rounded-circle" style="color: #06109d;"></i>
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                   <%= link_to facility_facility_path(current_facility.id),class: "btn btn-light" do %>
                     <i class="fas fa-user-cog mr-2"></i>マイページ
                    <% end %>
                    <div class="dropdown-divider"></div>
                    <%= link_to facility_animals_path, class: "btn btn-light" do %>
                      <i class="fas fa-paw mr-2"></i>投稿一覧
                    <% end %>
                    <div class="dropdown-divider"></div>
                    <%= link_to destroy_facility_session_path, method: :delete, class: "btn btn-light" do %>
                      <i class="fas fa-sign-out-alt mr-2"></i>ログアウト
                    <% end %>
                  </div>
              </li>

            <!--個人側ログイン後表示-->
          <% elsif reader_signed_in? %>
            <div class="pl-1"></div>
              <li class="nav-item dropdown">
                <a class="nav-link" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  <i class="far fa-user-circle fa-3x bg-light rounded-circle" style="color: #06109d;"></i>
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                   <%= link_to reader_reader_path(current_reader.id),class: "btn btn-light" do %>
                     <i class="fas fa-user-cog mr-2"></i>マイページ
                    <% end %>
                    <div class="dropdown-divider"></div>
                    <%= link_to reader_animals_path, class: "btn btn-light" do %>
                      <i class="fas fa-paw mr-2"></i>投稿一覧
                    <% end %>
                    <div class="dropdown-divider"></div>
                    <%= link_to destroy_reader_session_path, method: :delete, class: "btn btn-light" do %>
                      <i class="fas fa-sign-out-alt mr-2"></i>ログアウト
                    <% end %>
                  </div>
              </li>
              <%= form_tag(search_path, method: :get) do %>

            　　<%= select_tag 'range' ,options_for_select([['---選択してください---', ''], ['施設名', '1'], ['犬', '2'],['猫','3']]) %>
            
               　　<%= select_tag 'search', options_for_select([["前方一致","forward_match"], ["後方一致","backward_match"], ["完全一致","perfect_match"], ["部分一致","partial_match"]]) %>
               　　<%= text_field_tag (:word) %>
               　　<%= submit_tag "検索" %>
            
               <% end %>

              <!--ログイン前-->
          <% else %>
            <!--<li class= "nav-item">-->
            <!--  <%#= link_to new_facility_registration_path, class: "btn btn-light mr-3" do %>-->
            <!--     <i class="fas fa-paw"></i><br>施設新規登録-->
            <!--  <#% end %>-->
            <!--</li>-->
            <li class= "nav-item">
              <%= link_to new_facility_session_path, class: "btn btn-light mr-3" do %>
                <i class="fas fa-sign-in-alt"></i><br>施設ログイン
              <% end %>
            </li>

            <!--<li>-->
            <!--  <%#= link_to new_reader_registration_path, class: "btn btn-light mr-3" do %>-->
            <!--    <i class="fas fa-users-cog"></i><br>新規登録-->
            <!--  <%# end %>-->
            <!--</li>-->
            <li>
              <%= link_to new_reader_session_path, class: "btn btn-light" do %>
                <i class="fas fa-sign-in-alt"></i><br>ログイン
              <% end %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
  </header>